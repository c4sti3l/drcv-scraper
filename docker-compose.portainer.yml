services:
  drcv-livetiming:
    image: ghcr.io/c4sti3l/drcv-scraper:latest
    container_name: drcv-livetiming
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - drcv-data:/app/data

volumes:
  drcv-data:
